# Orbag — project status

_Living progress doc. Connected to the claude.ai Orbag Project via the GitHub connector so "where we are" is always visible there._

**Last updated:** 2026-06-07 (WORKORDER executed — BLOK 1 reporting-structure foundation (`reports` analytical columns + cross-vocabulary `match_key`/`report_match_keys`, gate passed buyer↔farmer); BLOK 3 country structure EU-27+UK+NA with fail-loud `country_benchmark_status`; BLOK 4 `intelligence_sources` legality registry with a hard DB invariant + `crawler_source_queue`; BLOK 2 match datamodel + worked net-value-per-tonne example (scoring weights await Leo). 6 migrations applied. **Then: n8n crawler repointed from a direct POST to the `upsert_intelligence_signal()` RPC + live benchmarks, and a full-repo bug-audit (round 1) — ~10 verified bugs fixed (0%-premium override, match region clusters, per-country customs, signup hardening, AI-key guards, alert-email escaping, locale, …) on branch `bugfix-audit-round1`; match-engine RLS + openEO denominator flagged to Leo.** All four test suites green; earlier today: Farmer BC inhaalslag: `api/farmer.js` herschreven naar een deterministische JS-transitie-engine — meerjarig cashflow/payback-model, AI schrijft alleen narrative, regressie-fixtures, `net_value` nu opgeslagen; + consistentie-fix: certificering toegevoegd aan My Account + prefill; earlier today: CFO-rapport view; crop-concentration worldwide foundation; buyer-location field + fixtures. All deployed and verified live on orbag.online)

**Earlier 2026-06-07 entry:** (CFO-rapport view: the buyer report now surfaces the engine's seven risk layers as a cost-of-inaction CFO view — headline trio, risk-layer table, risk→net-value bridge, justified farm-gate price, CFO callout; earlier today: crop-concentration worldwide provider foundation via CDSE openEO — Tier-1 NL BRP, Tier-2 EU CLMS, Tier-3 global WorldCover; buyer-location field + engine regression fixtures + "Adding a new region" docs; all deployed and verified live on orbag.online)

Legend: ✅ done & verified · 🟡 in progress · ⬜ planned / backlog

---

## 1. Platform & UI
- ✅ **English-consistent UI** across all pages (`boer`, `account`, `cockpit`, `zaden` translated; data/API keys untouched; `en-GB` date/number locale).
- ✅ **Auth/role fix** — `profiles.role` only drives prefill + default landing page, never gates navigation; any logged-in user can run both farmer and buyer analysis.
- ✅ **My Account role switch** so buyers see buyer company info.
- ✅ **Report bug fixes** in the buyer/farmer flows.
- ⬜ **Language switcher (NL/EN i18n)** — deliberately deferred; target audience is Dutch, so a switcher is the eventual goal. Not built yet.

## 2. Self-sharpening intelligence pipeline (Supabase)
The core automated system. Daily closed loop, no human needed in the normal case:

```
04:30 UTC  detect  →  intelligence-fx-weather   (ECB FX + Open-Meteo) writes signals (pending_review)
04:35 UTC  market  →  intelligence-market       refreshes MARKET.* (diesel via CBS, gated)
04:45 UTC  apply   →  apply_intelligence_updates() sharpens benchmarks, dedupes, holds outliers
04:50 UTC  notify  →  notify-held-signals        emails an alert when a signal is held
```

- ✅ **Detector** (`intelligence-fx-weather`, edge fn) — scheduled via pg_cron job `orbag-intelligence-fx-weather-daily`.
- ✅ **Apply step** (`apply_intelligence_updates()`, plpgsql, SECURITY DEFINER) — pg_cron job `orbag-intelligence-apply-daily`. Applies newest HIGH-confidence signal per `field_path` to `intelligence_benchmarks`, supersedes duplicates.
- ✅ **Guardrails** (autonomy + safety): HIGH-confidence only; `pct` values clamped 0–1; moves beyond 3× are **held** for manual review instead of applied. Verified with a synthetic outlier.
- ✅ **Weather self-sharpening** → `RISK_PCT.weather_conv.<country>` benchmarks.
- ✅ **FX self-sharpening** → new `LOGISTICS.import_cost.<country>` benchmarks track the live ECB rate, while `FX.EUR_*` 2024 baselines stay intact so the import-cost risk signal keeps firing.
- ✅ **Audit log** `intelligence_apply_log` — one row per run, `held_details` lists every guardrail-blocked outlier with reason.
- ✅ **Email alerts** via Resend → `leojansen@orbag.nl`, from `alerts@orbag.nl`. Fires only when something is held. **Verified end-to-end** (domain verified, secret set, test alert delivered).

## 3. Buyer business-case engine (`api/generate.js`)
JS engine computes every number; Haiku only writes the narrative (two-call design).
- ✅ **Coefficient calibration via deep research** — shock scenarios (Hormuz/Red Sea/energy) sourced to EIA/IRU/Eurostat/Drewry/ITF-OECD; logistics layer calibrated, **CO₂ + fuel mode-split road/sea** (CE Delft/GLEC/Eurostat); fuel intensity sourced; spoilage/packaging/returns confirmed unsourced and labelled LOW (no public primary exists).
- ✅ **Regional crop-data layer** — KWIN relabelled **NL-only quality table** (tare/defect/nitrate/dm); `REGIONAL_CROP_DATA` overrides yield/price/variable-cost per production region. `cropCoefficients()` merges them, per-field null-safe.
- ✅ **UK/Norfolk source** added (AHDB Farmbench / Grain Market Daily) + a two-click **Bolwick demo preset**; benchmark source label surfaced in the report KPIs and narrative.
- ✅ **Buyer-vs-farm separation** — production data is tied to where the crop is grown; logistics + customs are a function of the buyer (`customsCost()` gates import duty by EU-membership vs the buyer), so UK→NL pays the GB→EU charge while UK→UK would not.
- ✅ **Buyer location is a request field** (`buyerLocation` in `analyse.html`, default NL) — `customsCost()` is buyer-aware so UK→NL pays the GB→EU duty (€45/t) while UK→UK pays none. Back-compat: missing/unknown ⇒ EU buyer, so existing submissions are byte-identical. Live-verified end-to-end on orbag.online.
- ✅ **Engine regression fixtures** (`tests/engine.test.js`, run `node tests/engine.test.js`) — offline, no network; lock the computed nl / uk→nl / uk→uk numbers so a future change can't silently shift an existing farm's figures. Run before any engine change.
- ✅ **Crop-concentration risk layer** (todo2, `crop_concentration`) — same-crop area share within a per-crop pest radius → low/medium/high risk add-on. Enrichment: activates only when the request carries `farm_lat`/`farm_lon` (fixtures unaffected). NL source = **live BRP (PDOK/RVO WFS)** with a density preflight — computes live on feasible radii, defers with the exact parcel count where too dense (15 km in Flevoland ≈ 43k parcels → offline precompute needed; PDOK WFS has no server-side crop filter). Dronten example set drives offline tests (`node tests/concentration.test.js`). NDVI/Sentinel deferred. uk/dk/pt parcel sources are fail-loud stubs.
- ✅ **Worldwide concentration foundation** — a tiered provider registry (`CONCENTRATION_PROVIDERS`) + resolution chain (`resolveConcentrationGlobal`): Tier 1 per-country parcel data (HIGH; `nl-brp` live), Tier 3 cropland-density proxy (LOW, global) and **Tier 2 EU real crop-type (MEDIUM)** are **now wired via CDSE openEO precompute** — one openEO call computes the class fraction within the radius for a whole grid of farms, cached for credential-free reads. Tier-3 = ESA WorldCover (verified NL grid + FR Beauce 89%); Tier-2 = CLMS Crop Types Europe 10m, with the potato code (1210) **empirically validated against BRP ground truth + a wheat-belt control** (seeded for the Kempen). openEO needs a one-off device-flow login; production only reads the cache. Any farm worldwide gets a result labelled by **provider + method + confidence + coverage**, degrading to an explicit tiered "unavailable" naming what would serve it — never silent/faked. The lat/lon-keyed cache is the universal global store (now carries `source_id`/`granularity`/`confidence`/`country`, migration `extend_concentration_grid_provenance`). Verified: NL Dronten → HIGH parcel cache; FR → honest tiered unavailable.
- ✅ **Offline BRP precompute** (`scripts/precompute-concentration.js` + `crop_concentration_grid` table + `save_concentration_grid` RPC, migration `create_concentration_grid`) — downloads BRP once (paged), computes the true area-based share per grid point, caches it. The engine reads the nearest cell FIRST, so the **15 km case now computes live from cache** instead of deferring (PDOK WFS can't return 46k polygons in one serverless call). Seeded for the Dronten area (potatoes); real 15 km share there ≈ 16% (low), 34.8k parcels. Widen `GRID_N` / re-run at other centres to extend; full-NL scale → PostGIS bulk-load later. Re-run yearly (BRP is annual).
- ✅ **CFO-rapport view** (todo3) — the buyer report now surfaces the engine's already-computed risk layers as a *cost-of-inaction* CFO view at the top of `analyse.html`: a headline trio (cost of inaction / regen exposure / net value), the "seven risk layers" table with a total row, a "risk reduction → net value" bridge that arithmetically sums to net value (carbon shown separately as upside outside net value, since the engine excludes it), the justified farm-gate price, and a one-line CFO callout. `generate.js` exposes a compact `cfo` block (raw numbers + `justifiable_farm_price`) and the Haiku `verdict_reason` is reframed to open with the annual cost of standing still. Print-friendly (`@media print` extended, `nl-NL` number formatting). Work order + status: `operations/todo3-cfo-rapport-view.md`.
- ✅ **Concentration agronomy knobs calibrated** (still `verified:false`) — **radius** potatoes 15 km, MEDIUM confidence (≥16 km late-blight separation + 10–20 km wind dispersal; Firester 2018 / APS / Hannukkala 2007); **threshold curve** is a three-level step model (justified: invasion is a percolation phenomenon, critical host fraction ~0.33–0.41), positions MEDIUM (`medium 0.30 / high 0.50`), add-on magnitudes (1/3/6%) LOW/expert-prior. Each carries source + confidence in code; validate with a plant pathologist before treating as real.

## 3b. Farmer transition business-case engine (`api/farmer.js`)
Brought up to the buyer engine's standard (todo4): a **deterministic JS engine computes every number; the AI (`claude-sonnet-4-6`) writes the narrative only** — was previously a single AI call that both reasoned the economics and wrote prose (non-deterministic, untestable).
- ✅ **Structured benchmark data** — `KWIN_FARMER` (9 crops keyed on the boer.html Dutch crop values, EUR, `verified` flags; barley / sugar-beet premium / onion cost are `verified:false` estimates). Carbon recalibrated to the buyer engine's sourced €7-15/ha (supersedes the old unsourced €30-80/ha blob).
- ✅ **Multi-year transition model** (`TRANSITION_MODEL`, all knobs `verified:false`) — `calculateFarmer()` computes a year-by-year cashflow over a 7-yr horizon (yield-dip years 1-2, premium/input-saving/carbon ramp, transition capex), then derives **payback year, worst-year income dip, steady-state regen income and net value**. Verdict decided in JS from the farmer's payback target + income-drop tolerance. Cost-of-waiting overlay (conventional margin erosion + input inflation) mirrors the buyer's cost-of-inaction framing.
- ✅ **`farmer_bcs.net_value` is now persisted** (was `null`) — the match engine can finally see farmer economics.
- ✅ **boer.html upgraded** — multi-year cashflow strip with a payback marker, per-crop economics table, worst-year dip callout vs tolerance, cost-of-waiting callout, headline trio; print-friendly, `nl-NL` formatting.
- ✅ **Consistency fix (the named bug):** certification was referenced in the report but not settable in My Account → added a certification field to the farmer profile (`profiles.certification`, migration `add_certification_to_profiles`), persisted and prefilled into boer.html. Soil/region prefill verified (already aligned via `REGION_MAP`). Crop vocabulary deliberately left as buyer-keys in `profiles.crops` — `match.html` matches on it against `buyer_bcs.crop`, so changing it would break matching (the `CROP_MAP` bridge in boer.html is correct).
- ✅ **Regression fixtures** — `tests/farmer.test.js` (run `node tests/farmer.test.js`): baseline Groningen wheat+beet 85 ha locks verdict/payback/regen income/dip/net_value, plus determinism + fail-loud (unknown/empty crop). `engine.test.js` + `concentration.test.js` stay green.
- ⚠ Open: cost-of-waiting frame is on by default (confirm if undesired); sugar-beet/barley aren't in the buyer match vocabulary (buyer engine doesn't model them); all transition knobs `verified:false` (validate with an agro-economist). Work order + status: `operations/todo4-farmer-bc-engine.md`.

## 3c. Reporting structure as analytical foundation (WORKORDER BLOK 1)
The `reports` table now carries the analytical fields as typed, indexed columns instead of only inside the JSON blob, and a buyer report can be linked to a farmer report over the same crop+country.
- ✅ **Promoted columns** (migration `blok1_report_analytics`, applied + committed) — `country`, `buyer_location`, `justified_farm_gate_price`, `risk_reduction`, `report_type` (enum `buyer`/`farmer`), all backfilled from the existing 39 rows (all have `country`; `risk_reduction`/`justified_farm_gate_price` only where the CFO block existed). Raw input/output JSON stays intact in `input_data`/`report_data` (dead storage, untouched).
- ✅ **Cross-vocabulary linkage** — `match_key` (generated column `orbag_match_key(crop,country)`) + the `report_match_keys` view that explodes multi-crop farmer rows one-per-crop. `orbag_canonical_crop()` bridges the farmer (Dutch: `wintertarwe`) and buyer (English: `wheat`) crop keys; `orbag_supply_country()` normalises free-text regions to a country. **Deviation from the literal workorder formula** (`crop:region`), which provably links nothing because the two engines use different crop *and* region vocabularies — joining on canonical-crop + country achieves the gate's intent. region stays a typed/indexed column for filtering, not as the join key.
- ✅ **GATE PASSED** — `report_match_keys` joins existing buyer↔farmer reports on a shared `match_key` (e.g. `green_beans:nl`: Bonduelle/bulk buyers ↔ farmer Akkerhoeve; also `potatoes:nl`, `wheat:nl`). Verified live.
- ✅ **API writes the columns going forward** — `save_report` RPC extended; `generate.js` (buyer) sets `report_type`/`country`/`buyer_location`/`risk_reduction`/`justified_farm_gate_price` via a new `supplyCountry()` helper (SQL-twin of `orbag_supply_country`); `farmer.js` sets `report_type='farmer'`/`country`. RPC round-trip verified (generates `wheat:nl`); all three offline test suites stay green.
- ⚠ Open gate-question for Leo: confirm the canonical-crop + country linkage basis (vs the literal `crop:region`), and the `no_preference → nl` home-market default.

## 3d. Country structure for Europe + North Africa (WORKORDER BLOK 3)
The code already handles all of Europe + NA; only benchmark DATA is missing. The empty-but-ready country structure is now in place so an incoming benchmark is live without a new migration.
- ✅ **Country registry** (migration `blok3_country_structure`, applied + committed) — `intelligence_countries` extended with `iso_code`, `currency`, `region_cluster`, `benchmark_source_planned`, `engine_region_key`; seeded **EU-27 + UK + Morocco/Egypt/Tunisia** (31 countries) with centroid lat/lon and `fx_pair` for non-EUR currencies. All `rotation_active=false` (out of the daily crawl until a source is wired in BLOK 4); the legacy region buckets (`netherlands`, `morocco_egypt`, …) are untouched. Nothing in the app reads this table for dropdowns, so the seed is inert until activated.
- ✅ **Per-country own-benchmark principle** — `benchmark_source_planned` carries NL=KWIN-AGV, UK=AHDB, DK=SEGES, PT=COTHN/GPP; the rest = `UNASSIGNED` until chosen. `region_cluster` is descriptive only (commented: must never be used to borrow another country's benchmark).
- ✅ **GATE PASSED — fail loud, no fabrication** — no placeholder benchmark rows were inserted (`intelligence_benchmarks.current_value` is NOT NULL, so a placeholder would be a fabricated number). The *absence* of a real row IS the unavailable state, surfaced by the new `country_benchmark_status` view: NL/UK → "own benchmark available"; DE/FR/DK/PT/TN → **"no benchmark available — FAIL LOUD: do NOT fall back to another country"** (naming the planned source where known). Verified live.
- ⚠ Out of scope (flagged): the engine (`generate.js`) still has an NL fallback for provisional regions (by design, per "Adding a new region"). Wiring the fail-loud principle INTO the engine for activated countries is a separate, gated change — not touched here.

## 3e. Legal data sources + crawler foundation (WORKORDER BLOK 4)
The crawler may only ever touch a source registered with an explicit licence. That registry + its legality invariant now exist; the live crawl stays Leo-gated.
- ✅ **`intelligence_sources` table** (migration `blok4_intelligence_sources`, applied + committed) — per-country source registry: `source_type` (api/rss/scrape), `url`, `license`, `scraping_allowed` (default false), `confidence_tier`, `legality_class` (green/orange/red), `signal_types[]`. RLS anon-read (no secrets, just public URLs + licences).
- ✅ **Gate as a HARD DB invariant** — `scraping_allowed=true` is only permitted with an explicit licence and a non-RED class. **Verified:** 12 green (all allowed) / 3 orange (0 allowed) / 3 red (0 allowed); a direct UPDATE allowing Reuters (red) is rejected with a check-constraint violation. RED can never be auto-crawled.
- ✅ **Legality classification seeded** — GREEN (Eurostat, EC DG-AGRI, ECB, FAOSTAT, World Bank, USDA ERS, Open-Meteo, CBS, RVO/PDOK, ONS, Destatis, INSEE — each with its real open licence); ORANGE (AHDB/SEGES/COTHN-GPP — national benchmark sources awaiting a manual ToS check); RED (DCA/Reuters/Bloomberg — never auto-extracted).
- ✅ **`crawler_source_queue` view** — the crawler's daily worklist: per country, ordered API→RSS→scrape, only `active AND scraping_allowed`. The n8n workflow reads this and writes each signal via `upsert_intelligence_signal()` (NOT a direct POST); apply-step unchanged. Contract documented in `operations/blok4-crawler.md`.
- ✅ **n8n crawler POST→RPC done** (after Leo enabled MCP access) — the active *"Orbag Intelligence Crawler"* (`KLNN5WeBC8ZE4GUI`) still did a **direct POST** to `/rest/v1/intelligence_updates` (the workorder's `[x]` was wrong). Now repointed to `…/rpc/upsert_intelligence_signal` with `p_*` params + `p_generated_by:"n8n-crawler"`; jsonb arrays sent un-stringified; the digest reads the split node (RPC returns only `{id,action}`). Also: `current_value` is now read **live** from `intelligence_benchmarks` (defensive snapshot fallback), and GDELT + World Bank registered in `intelligence_sources`. RPC mapping verified via SQL. See `operations/blok4-crawler.md`.
- ⛔ **Leo-gated:** (1) going live on a NEW source requires its explicit licence — GREEN cleared, ORANGE (AHDB/SEGES/COTHN-GPP) await your ToS go-ahead. (2) Credential-hardening: the anon-key is hardcoded in the node headers; the n8n MCP can't create credentials — make one Header Auth credential and I'll attach it (on `OPEN-VOOR-JOU.md`). (3) Full source expansion (an extractor per green source on `crawler_source_queue`) is incremental, per source.

## 3f. Match engine — from filter to scoring (WORKORDER BLOK 2, gated)
The current match (`match.html`) is a *filter* (crop membership + region overlap). BLOK 2 adds the economics that turn it into *scoring*: net value per tonne to the buyer after logistics. Per the gate, only the **data model + one worked example** landed; the **scoring weights wait for Leo** (a business decision).
- ✅ **Data model** (migration `blok2_match_datamodel`, applied + committed) — `buyer_bcs` gained seasonality, delivery_spec, quality_spec, certification_required, contract_preference, risk_sharing, traceability_level, buyer_lat/lon, buyer_location; `farmer_bcs` gained deliverable_volume_t, harvest_window, quality_spec, certification, contract_preference, transition_phase, farm_lat/lon, country (the niveau-1/2 inputs). All nullable → existing rows + the current filter are unchanged. Niveau-3 (concentration/growth/geopolitical complementarity) stays DERIVED, not stored. `save_buyer_bc`/`save_farmer_bc` RPCs extended to persist them.
- ✅ **Logistics submodule + worked example** (`api/match-economics.js`, `tests/match.test.js`) — `logisticsCostPerTonne(farmer,buyer)` (road distance × fuel intensity ÷ load + handling + cold chain + cross-border customs, all mirrored from the buyer engine) and `netValuePerTonne(justifiedFarmGatePrice,…)`. **Worked example:** NL Dronten farm → NL Rotterdam green-bean buyer (€413/t justified) → **€398.83/t net** (108 km, customs €0); the same buyer from a UK/Norfolk farm → **€344.70/t net** (customs €45/t). So matches are now comparable on net value to the buyer, not just availability. Offline-deterministic; all four test suites (engine/farmer/concentration/match) green.
- ⛔ **GATE — awaiting Leo:** the full multi-variable scoring with WEIGHTS across niveau 1-3 (volume-fit, season-fit, quality, certification, contract, risk-sharing, concentration, growth, geopolitical complementarity) is a business decision and is deliberately NOT wired. Decide the weighting and I'll ring it in + surface ranked matches in `match.html`.

## 4. International & self-bootstrapping regions
Adding a country is a DB row (or an auto-bootstrap), not a code deploy.
- ✅ **DB-backed regions** — `region_coefficients` (risk + logistics + customs per source) and `regional_crop_data` (yield/price/cost per region), anon-read, seeded 1:1 from the old hardcoded values. Engine reads them live (`getRegionalData` + `buildRegions`) with the hardcoded constants as a **fallback** — never breaks if the DB is empty/unreachable. Verified: no-DB result byte-identical to before; DB overrides picked up live.
- ✅ **Auto-bootstrap** (`api/bootstrap-region.js`) — a new country resolves via World Bank metadata → a full **provisional** region: great-circle distance→logistics, EU-27 lookup→customs, income level→geo-risk proxy, WB cereal yield→crop yield proxy. **Verified live on Poland** (0 data → working buyer BC in minutes, transparently labelled provisional).
- ✅ **Signup trigger** — `account.html` captures Country → fire-and-forget `/api/bootstrap-region` (non-blocking); `profiles.country` column added. `analyse.html` populates the source dropdown from the DB so a new region is immediately selectable.
- ✅ **Write architecture (least-privilege)** — `SUPABASE_KEY` is the ANON key and is the only key in the app **by design** (no all-powerful service_role key = no such attack surface). All privileged writes go through bounded `SECURITY DEFINER` RPCs: `save_report` / `save_buyer_bc` / `save_farmer_bc`, `bootstrap_region`. This **fixed silent report-save failures** (anon could not insert into `reports`/`buyer_bcs`/`farmer_bcs`) — verified a report row now persists live.

## 5. Infrastructure & ops
- ✅ Supabase project "Project generator" (`wbysgkfsaoyaaatdgics`, West EU).
- ✅ 3 pg_cron jobs active (detect / apply / notify).
- ✅ Secret `RESEND_API_KEY` set in Supabase; `orbag.nl` verified as Resend sender domain.
- ✅ CLIs (`gh`, `vercel`, `supabase`) authenticated and resolving bare.

## 6. Repo & deploy
- 🟡 **Two unmerged branches from 2026-06-07** (main itself is at the pre-workorder commit):
  - `workorder-bloks-1-4` — WORKORDER BLOK 1-4 (report analytics, country structure, source legality, match datamodel) + the n8n crawler POST→RPC fix.
  - `bugfix-audit-round1` — branched **from** `workorder-bloks-1-4`, so it contains both; the bug-audit fixes sit on top. **Merge order:** merge `bugfix-audit-round1` last (or merge it alone — it carries everything). No PRs opened yet.
- ✅ **Migrations applied to the remote DB** (all committed on the branches above): the regional layer (`create_regional_data_tables`, `add_country_to_profiles`, `bootstrap_region_function`, `report_save_rpcs`) + today's `blok1_report_analytics`, `blok3_country_structure`, `blok4_intelligence_sources`, `blok2_match_datamodel`, `register_crawler_live_sources`, `fix_country_benchmark_status_like`.
- ✅ Serverless: `api/generate.js` (buyer), `api/farmer.js` (farmer), `api/bootstrap-region.js` (new-region auto-bootstrap), `api/match-economics.js` (match net-value-per-tonne, BLOK 2).
- ✅ **Test harness** — `node tests/{engine,farmer,concentration,match}.test.js` (no deps): offline regression fixtures (nl / uk→nl / uk→uk numbers, farmer cashflow, concentration, match economics incl. the 0%-premium + per-country-customs regressions). Briefing docs for autonomous sessions live under `operations/`.
- 🟡 **Partial migration history in repo** — session migrations are committed; 7 earlier ones exist only in the remote DB. Run `supabase db pull` for a full baseline.

---

## 4b. Bug-audit round 1 (2026-06-07)
Full-repo audit (5 parallel review agents over ~8k lines), findings verified against the code, fixes on branch `bugfix-audit-round1` (all four offline test suites green; new regression tests added).
- ✅ **Fixed (autonomous):** zero-premium silently became 15% (`generate.js` — `/100 || 0.15`, also the justified-price calc; +regression test); per-source customs duty in the match engine (`match-economics.js` was a flat €45 for every border incl. Morocco/Egypt — now mirrors `generate.js` import_cost; +test); `regionsOverlap` only bridged the noord cluster → added west/oost so West-NL/Zeeland/Flevoland matches aren't silently dropped (`match.html`); a missing AI key (`scenarios`/`next_steps`/`crop_recommendations`) threw away a paid report → defaults in `generate.js`/`farmer.js` + Anthropic `content` guard; signup hardened (`account.html` — guard `data.user`, session-aware redirect, deferred-profile via `ensureProfile()`, `maybeSingle()`); `getLiveBuyerContext` over-broad filter (`farmer.js`); bootstrap `lat && lng` truthiness faked 1500 km at the equator + cereal-yield now sorted by year (`bootstrap-region.js`); HTML-escape crawled text in the held-signal alert email (`notify-held-signals`, **deployed**); en-GB locale consistency (`analyse.html` CFO + `match.html`); `country_benchmark_status` LIKE now escapes the key (migration `fix_country_benchmark_status_like`, applied); BRP precompute fails loud on a non-OK page; openEO denominator caveat documented; carbon fallback 40→8.
- ⛔ **Needs Leo (on `OPEN-VOOR-JOU.md`):** **match-engine RLS** — `buyer_bcs`/`profiles` SELECT is `auth.uid()=id`, so cross-user matching is impossible and the match page is empty for everyone; opening it is a privacy/exposure decision. Plus the openEO concentration-denominator validation (coupled to the CDSE login).
- ℹ️ **Known/low, left as-is:** `cost_of_waiting.enabled=true` in `farmer.js` (already a decision on the checklist); a couple of dead client functions (`buildPrompt`/`renderRiskTable` in `analyse.html`); `save_report` response not logged (observability).

## Backlog / next steps
> 📋 **Wat op jóú wacht (credentials, dashboards, externe expertise, beslissingen): [`operations/OPEN-VOOR-JOU.md`](operations/OPEN-VOOR-JOU.md)** — één opvraagbare checklist.
- ⬜ **Crop-concentration roadmap** — the worldwide layer is built (Tier-1 NL BRP / Tier-2 EU CLMS / Tier-3 global WorldCover). Open follow-ups (roll out coverage, validate more CLMS crop codes, openEO login refresh, rotate the SH secret, PostGIS scale path, global Tier-2 WorldCereal, agronomy validation) are tracked in **`operations/todo2-gewasconcentratie.md` §I**.
- ⬜ **Sharpen provisional regions** — upgrade auto-bootstrapped regions from `provisional` → `active`: per-crop yield/price/variable-cost via FAOSTAT/Statista (through the Hanze library) + the deep-research harness. Bootstrap leaves price/variable-cost null on purpose (engine falls back to NL meanwhile).
- ⬜ **Rate-limit the public RPCs** (`save_*`, `bootstrap_region`) — they are anon-callable (parity with the already-public `/api/generate`); add edge rate-limiting before scale.
- ⬜ Optional: unlock the **regen baseline** (currently NL-locked) so a non-NL farm can be modelled as the regen target, not just the incumbent source.
- ⬜ Language switcher (NL/EN i18n).
- ⬜ `supabase db pull` to capture the 7 earlier migrations into the repo for a full baseline.
- ⬜ Optional: held-count badge in the cockpit dashboard.
- ⬜ Optional: weekly summary email (not just held-alerts).
- ⬜ Optional: `reply_to` on the alert email (left out in a revert).
- ⬜ Optional: "cool-down" on weather risk so it can't drift up indefinitely during a long drought.
- ⬜ Account hygiene: remove the stray `orbag.eu` domain from the Vercel "orbag" team — it is not in the code, not a project domain, and not serving anything (live runs on `orbag.online`); the CLI reports "no access", so it needs the Vercel dashboard (Team → Settings → Domains).

---

_Maintenance: update this file (and the date above) whenever a milestone lands, then commit. The connected claude.ai Project reads it from GitHub._
